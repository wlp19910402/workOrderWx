<view class="page__bd">
	<mp-cells>
		<mp-cell>
			<mp-uploader delete="{{true}}" binddelete="uploadDelete" bindfail="uploadError" bindsuccess="uploadSuccess" select="{{selectFile}}" upload="{{uplaodFile}}" files="{{imgUrls}}" max-count="6" title="图片上传" tips="最多只能上传6张"></mp-uploader>
		</mp-cell>
	</mp-cells>
</view>